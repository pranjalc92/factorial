#include<stdio.h>
 
 
int main()
{
    int x,n,z;
    
    scanf("%d",&n);
    while(n>0)
    {
              scanf("%d",&x);
      z=0;           
    while(x>0)
    {
    x=x/5;
    z=z+x;
      }
    printf("%d \n",z);
    n--;
    
    }
    return 0;
}
 